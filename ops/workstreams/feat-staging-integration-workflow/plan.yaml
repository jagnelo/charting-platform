schema: 2
branch: feat/staging-integration-workflow
base_sha: b54ccb5eb193aa2b0f8cc76f67dcc26e863951f9
parent_branch: fix/integration-candidate-lifecycle
parent_sha: b54ccb5eb193aa2b0f8cc76f67dcc26e863951f9
dependency_authorization: "The human explicitly authorized replacing local integration candidates with a staging branch and asked that all dependent workflows and tooling be updated. The dependent-branch policy was merged into this branch before implementation."
human_intent_authorization: "Keep RPi support but make its target-specific architecture checks explicitly deployment-triggered; replace local integration candidates with persistent staging and update all workflows, rules, policies, scripts, and commands thoroughly."
human_closure_authorization: pending
closure_summary: pending
validation_tier: full_integration
human_validation_authorization: "The human explicitly requested thorough final validation and verification; CI and deployment-contract changes require the full integration tier."
goal: "Make staging the single tested integration line, preserve green master, remove normal-CI architecture emulation, and select RPi architecture only in explicit deployment flows."
scope: ["staging integration and promotion", "worktree ancestry and closure", "GitHub CI", "migration baseline", "RPi target architecture", "agent and human workflow documentation", "exhaustive-gate persistence synchronization"]
owned_paths: [".gitignore", "Makefile", ".github/workflows/ci.yml", "scripts/staging.py", "scripts/integrate.py", "scripts/integrate-set.py", "scripts/worktree.py", "scripts/rpi.py", "scripts/validate-migration-compatibility.py", "scripts/validate-arm64-images.py", "deploy/rpi/rpi.env.example", "docs/agent-orchestration.md", "docs/deployment.md", "docs/development.md", "docs/worktree-lifecycle.md", "frontend/src/views/WorkstationView.vue", "frontend/tests/e2e/flows.spec.ts", "tests/workflow/test_staging_workflow.py", "ops/workstreams/feat-staging-integration-workflow"]
dependencies: ["feat/workflow-unification", "feat/dependent-branch-policy", "fix/integration-candidate-lifecycle"]
acceptance_criteria: ["ordinary branches start from green staging", "exact closed source SHAs merge once into persistent staging", "conflict restores staging and is resolved on source branch", "staging exhaustive CI gates master promotion", "master only fast-forwards to exact green staging", "normal CI has no QEMU or architecture-specific image build", "RPi platform is explicit and preflight-verified", "no new local integration candidate copies"]
branch_tests:
  - uv run --project backend python -m pytest tests/workflow/test_staging_workflow.py -q
  - uv run --project backend python -m py_compile scripts/staging.py scripts/integrate.py scripts/integrate-set.py scripts/worktree.py scripts/rpi.py scripts/validate-migration-compatibility.py
live_test_impact: "No provider calls; GitHub-run lookup/promotion paths require remote integration rehearsal after this workflow reaches master and staging is bootstrapped."
migration_impact: "No schema change; compatibility gate baseline changes from merge-parent inference to explicit previous release SHA."
deployment_impact: "RPi remains supported as linux/arm/v7 or linux/arm64, selected in ignored target config and built only by an explicit deployment request."
status: ready_for_human_review
remaining_gaps: ["Human review and explicit topic-closure authorization remain pending; this implementation request does not authorize integration.", "One-time human-confirmed staging bootstrap can occur only after this workflow reaches master.", "A live integration/promotion rehearsal requires human closure of a real source topic and a bootstrapped remote staging branch.", "The exhaustive gate exposed inherited F8e persistence, F9e delayed-focus, and F8s dock-activation races; their unchanged behavioural oracles now pass focused repetition and the final complete gate."]
