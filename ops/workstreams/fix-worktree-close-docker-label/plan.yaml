schema: 1
branch: fix/worktree-close-docker-label
base_sha: 946cd5308fe0a80cb9c01da4d0fbcc4053e615a9
goal: "Repair safe worktree closure Docker project inspection"
scope:
  - "Use a valid Docker label format template when proving managed projects are stopped."
owned_paths:
  - scripts/worktree.py
dependencies: []
acceptance_criteria:
  - "Safe close detects Docker projects without a template parse error."
  - "Safe close still refuses when this worktree has a running managed project."
branch_tests:
  - "python3 -m py_compile scripts/worktree.py"
live_test_impact: none
migration_impact: none
deployment_impact: none
human_closure_authorization: "The human explicitly authorized closure of workflow-only branches; the implementation is already published in green master and any remaining gaps stay documented for future work."
closure_summary: "Workflow-only branch fix/worktree-close-docker-label is closed as superseded: its implementation is already included in the published master line, and no product or deployment behavior remains active on this branch. Its source changes are reachable from master. The remote ref is retained as audit history; any future work must start from green staging."
status: closed
remaining_gaps: []
