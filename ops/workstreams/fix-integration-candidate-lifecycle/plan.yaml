schema: 2
branch: fix/integration-candidate-lifecycle
base_sha: 803d05acda9af22255f6c4ae7c020829e02e0f8a
parent_branch: feat/workflow-unification
parent_sha: 803d05acda9af22255f6c4ae7c020829e02e0f8a
dependency_authorization: "The human authorized autonomous completion of the workflow refinement and explicitly approved dependent branches when the dependency is documented."
human_intent_authorization: "Document and enforce an accounted lifecycle for temporary integration copies so stale or failed attempts cannot be forgotten."
human_closure_authorization: pending
closure_summary: pending
validation_tier: focused_only
human_validation_authorization: "The human authorized workflow/helper implementation to use focused verification before later master exhaustive replay."
goal: "Make every temporary integration candidate uniquely identified, audited, and removed unless actively retained for semantic conflict resolution."
scope: ["candidate identity", "local lifecycle ledger", "automatic post-failure removal", "legacy candidate classification", "agent workflow documentation"]
owned_paths: ["scripts/integrate.py", "scripts/integrate-set.py", "scripts/worktree-cleanup.py", "Makefile", "docs/agent-orchestration.md", "docs/development.md", "docs/worktree-lifecycle.md", "ops/workstreams/fix-integration-candidate-lifecycle"]
dependencies: ["feat/workflow-unification"]
acceptance_criteria: ["candidate identity includes exact master and source inputs", "every new candidate writes a lifecycle record", "ordinary failures are recorded then removed", "only explicit active conflict resolution can retain a candidate", "cleanup report flags unaccounted historical candidates"]
branch_tests:
  - PYTHONPYCACHEPREFIX=/tmp/charting-platform-pycache python3 -m py_compile scripts/integrate.py scripts/integrate-set.py scripts/worktree-cleanup.py
  - python3 scripts/worktree-cleanup.py report > /tmp/charting-candidate-lifecycle-report.json
  - python3 scripts/integrate.py --help
live_test_impact: none
migration_impact: none
deployment_impact: none
status: in_progress
remaining_gaps: ["Existing historical candidates require a separate reconciliation decision before removal because their exact merge resolution may be locally unique."]
