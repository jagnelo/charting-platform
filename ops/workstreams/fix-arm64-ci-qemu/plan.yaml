schema: 1
branch: fix/arm64-ci-qemu
base_sha: ca27acbf6ee09e543d57dbec4a7ffdf64c4c3b52
goal: "Make the authoritative Linux ARM64 image gate complete reliably on an x86 GitHub runner."
scope:
  - "Run the frontend static-asset builder on BUILDPLATFORM instead of under ARM64 QEMU."
  - "Keep the final nginx runtime image on TARGETPLATFORM so the produced image remains linux/arm64."
  - "Add a deployment contract test preventing regression to emulated Node builds."
owned_paths:
  - frontend/Dockerfile
  - tests/deployment/test_rpi_contract.py
  - ops/workstreams/fix-arm64-ci-qemu/
dependencies:
  - "master at ca27acbf6ee09e543d57dbec4a7ffdf64c4c3b52"
  - "The existing validate-arm64-images build and master CI replay"
acceptance_criteria:
  - "Targeted deployment contract tests pass."
  - "Frontend linux/arm64 image builds without executing Node through QEMU."
  - "Full local integration gate passes."
  - "The exact merged master SHA receives a green independent GitHub replay."
branch_tests:
  - "backend/.venv/bin/pytest -q tests/deployment/test_rpi_contract.py"
live_test_impact: none
migration_impact: none
deployment_impact: "Restores the ARM64 image prerequisite for a future human-authorized RPi bundle; no deployment is automatic."
human_closure_authorization: "The human explicitly authorized closure of workflow-only branches; the implementation is already published in green master and any remaining gaps stay documented for future work."
closure_summary: "Workflow-only branch fix/arm64-ci-qemu is closed as superseded: its implementation is already included in the published master line, and no product or deployment behavior remains active on this branch. Its source changes are reachable from master. The remote ref is retained as audit history; any future work must start from green staging."
status: closed
remaining_gaps:
  - "The physical RPi rehearsal remains blocked until deployment configuration, SSH trust, and direct human authorization are provided."
