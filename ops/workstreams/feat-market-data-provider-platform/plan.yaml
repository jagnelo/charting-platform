schema: 4
branch: feat/market-data-provider-platform
base_sha: 8b885a2ffd9cbb8b20c626e2c0381d3fce5cdc35
parent_branch: staging
parent_sha: 8b885a2ffd9cbb8b20c626e2c0381d3fce5cdc35
dependency_authorization: "not applicable: independent staging-based topic"
human_intent_authorization: "Implement the approved US-first multi-provider market-data platform plan: canonical FIGI-based identity and issuer model; exchange/session calendars; market-series scoped raw/canonical OHLCV with local rollups and adjustments; provider capability/entitlement/quota routing; US universe/lifecycle, events, SEC fundamentals, FINRA short interest, current options with local high-fidelity Greeks, optional futures and crypto providers; backend admin/diagnostic APIs; and disabled 30-day shadow monitoring. Backend/data only; do not modify frontend or ETF constituent provider work."
planning_state: ready
human_closure_authorization: pending
closure_summary: pending
validation_tier: full_integration
human_validation_authorization: agent_default_full_integration
local_validation_profile: full_stack_browser
local_validation_reason: backend application, database migrations, provider adapters, Redis-backed runtime controls, worker scheduling, and Compose secret boundaries require the complete stack gate even though no frontend product files are in scope.
goal_budget_policy: unbounded_unless_human_authorized
human_goal_budget_authorization: none
goal: "Implement the approved US-first multi-provider market-data platform plan: canonical FIGI-based identity and issuer model; exchange/session calendars; market-series scoped raw/canonical OHLCV with local rollups and adjustments; provider capability/entitlement/quota routing; US universe/lifecycle, events, SEC fundamentals, FINRA short interest, current options with local high-fidelity Greeks, optional futures and crypto providers; backend admin/diagnostic APIs; and disabled 30-day shadow monitoring. Backend/data only; do not modify frontend or ETF constituent provider work."
scope:
  - canonical FIGI-domain identity, issuer/legal-entity separation, effective-dated listings and provider symbols, and unresolved-identity quarantine
  - market-series scoped OHLCV observations and canonical bars with session, feed, adjustment, local aggregation, and coverage provenance
  - versioned exchange sessions, holidays, early closes, timezone projections, and exchange-aware freshness
  - provider capability/entitlement descriptors, durable quota reservations, usage accounting, health, routing, workload leases, and shadow telemetry
  - US universe/lifecycle reconciliation and backend data ingestion for events, SEC facts, earnings, FINRA short interest, options, futures, and crypto providers
  - QuantLib-backed calculated option Greeks with explicit model/input provenance and existing Black-Scholes fallback
  - backend-only coverage, provider-admin, identity-quarantine, market-session, and shadow-report APIs; symbol compatibility aliases remain additive
  - automated shadow-mode reporting and post-deployment activation evidence; no frontend or ETF constituent adapter implementation
owned_paths:
  - .env.example
  - .github/workflows/provider-live.yml
  - backend/app/models/
  - backend/app/providers/
  - backend/app/services/
  - backend/app/routers/
  - backend/app/schemas/
  - backend/app/tasks/
  - backend/app/workers/
  - backend/alembic/
  - backend/tests/
  - deploy/rpi/compose.yml
  - docker-compose.yml
  - docs/architecture.md
  - docs/data-providers.md
  - docs/deployment.md
  - docs/development.md
  - docs/provider-live-validation.md
  - docs/project-todos.md
  - ops/workstreams/feat-market-data-provider-platform/
  - scripts/run-live-provider-probes.py
  - scripts/merge-provider-live-usage.py
  - scripts/worktree-runtime.py
dependencies:
  - staging baseline at 8b885a2ffd9cbb8b20c626e2c0381d3fce5cdc35
  - feat/etf-holdings-constituents owns ETF constituent provider adapters; this branch consumes compatible canonical identities only
  - feat/etf-holdings-constituents is active in parallel and may overlap on ETF identity/discovery evidence; reconcile contracts at integration without changing its adapter ownership
  - configured provider credentials and terms are explicit; unavailable or quota-unknown providers remain visible but non-routable and do not block unrelated providers; account-wide usage outside the shared application database is only trusted when a provider-native cumulative usage observation is reconciled
acceptance_criteria:
  - id: AC-IDENTITY
    text: New and reconciled US-listed securities use stable FIGI-domain keys where applicable, issuer identity is separate from security identity, effective-dated listing/provider-symbol history is retained, and unresolved candidates are quarantined without silent merges.
  - id: AC-SERIES
    text: Canonical bars distinguish instrument, market/feed scope, session, timeframe, adjustment basis/version, and provenance; existing OHLCV reads remain compatible through a default series mapping.
  - id: AC-CALENDAR
    text: Exchange sessions, timezones, holidays, early closes, overnight trade-date rules, and user-local projections are versioned and used by freshness and daily-completion logic.
  - id: AC-ROUTING
    text: Provider routing filters by capability, asset/venue/session/interval/history/feed/terms entitlement, health, quota, and workload budget; quota reservations are safe across workers and every decision is observable.
  - id: AC-REFRESH
    text: Whole-universe core-session D1 refresh, tiered intraday polling, local rollups, exact missing-slice repair, request coalescing, deferred broad-evaluator runs, and automatic retry are implemented without provider calls inside evaluation loops.
  - id: AC-DATA
    text: Provider adapters cover the approved practical roster, with US identity/lifecycle, OHLCV, corporate actions, market events, SEC facts/core fundamentals, earnings, FINRA short interest, crypto, current options, and optional futures paths represented honestly by capability and provenance.
  - id: AC-OPTIONS
    text: Current option chains and quotes are retained; QuantLib-backed American-option Greeks and the existing fallback are labeled with model/version/input provenance and tested against numerical edge cases.
  - id: AC-OPS
    text: Backend-only admin and diagnostic APIs expose capabilities, entitlements, quota/cost usage, routing decisions, coverage, identity quarantine, sessions, queue/degradation state, anomalies, and detailed shadow reports.
  - id: AC-SHADOW
    text: New routing defaults remain disabled until a separately approved deployed 30-day shadow run produces automated daily/final reports, at least 99% eligible core-session D1 coverage before the next regular session, no silent identity/license/quota violations, and AI-readable discrepancy evidence.
  - id: AC-COMPAT
    text: Existing symbol-based API aliases and legacy yfinance-dependent paths remain functional only through explicit compatibility behavior; yfinance is absent from default and acceptance routing.
  - id: AC-TEST
    text: Focused unit, adapter-fixture, migration, PostgreSQL/Redis integration, worker, API, numerical, point-in-time, and full backend integration validation pass; every live-enabled provider has either bounded keyless evidence or an explicit credential preflight failure; no frontend files are modified.
  - id: AC-QUOTA-SAFETY
    text: No provider policy contains a generic 60/min, 15-burst, 2-concurrency, or 30-second fallback. Unknown limits are represented as NULL/unknown, excluded from routing, and surfaced with the exact missing contract dimensions.
  - id: AC-RATE-FAILURE
    text: 429/418/quota responses parse provider reset headers where present, stop the affected provider scope, persist a typed capacity event, defer work until the reset (or operator decision), and do not create a retry storm or unsafe fallback.
  - id: AC-LIVE-EVIDENCE
    text: The manifest-driven live matrix covers keyless and credentialed providers separately. Missing credentials identify exact environment variables and block ready-for-review; no live skip is represented as proof.
  - id: AC-SECRET-DISTRIBUTION
    text: Local worktrees, GitHub live validation, and deployment targets use separate explicit secret stores; no credential is tracked, routine PR CI receives no provider secret, and provider credentials reach only trusted backend/worker processes.
branch_tests:
  - backend unit and provider fixture suites for identity, routing, calendars, coverage, events, adjustments, options, and anomaly handling
  - backend migration compatibility and PostgreSQL/Redis integration tests
  - worker scheduling, quota reservation, request coalescing, deferred-run retry, and provider health tests
  - backend API contract tests for canonical IDs, compatibility symbol aliases, coverage, provider admin, sessions, quarantine, and shadow reports
  - opt-in live provider probes plus full backend integration validation when credentials and infrastructure are available
branch_tests_reason: Application models, migrations, providers, workers, Redis quota controls, and backend APIs are all changed; full backend integration is required. Frontend/browser validation is out of scope by explicit authorization.
live_test_impact: opt-in provider probes and a separately approved post-deployment 30-day disabled shadow run; no deployment or activation is authorized in this implementation session
migration_impact: multiple additive/transformational PostgreSQL migrations for issuers, domain identities, series scope, sessions, facts/events/short interest, provider runtime budgets, and shadow telemetry; old symbol/OHLCV rows require deterministic compatibility backfill
deployment_impact: new backend/worker configuration, optional provider credentials, QuantLib dependency, Redis-backed quota state, scheduled shadow-report jobs, and feature flags; deployment remains a separate human-authorized master workflow
status: in_progress
remaining_gaps:
  - the latest forced live matrix at 2026-09-10T07:03:12Z collected 34 cases with 31 passing in 40.75 seconds after temporary non-secret EDGAR_USER_AGENT and MARKETSTACK_DISCOVERY_EXCHANGE=XNAS overrides; the existing external env still lacks the three exact credential domains ALPACA_API_KEY/ALPACA_SECRET_KEY, TRADIER_API_KEY, and MARKETDATA_APP_API_KEY. All seven tokenized live probes passed, and the changed Binance/Nasdaq/FINRA paths have current end-to-end evidence; the wrapper returned exit code 2 with no acceptance claim. The updated Massive IPO-calendar case passed within the 31, the FMP profile/history/earnings-calendar path passed positive live validation, and the Alpha Vantage IPO-calendar live case passed transport/schema validation against a valid header-only CSV response but observed no IPO rows; a positive event row remains unobserved and no synthetic event is permitted. The safety preflight reports FRED's three explicit missing review controls and FINRA OTC's five explicit source/cost controls. Each environment must configure its own SEC contact value.
  - the checked-in manual GitHub live workflow is environment-scoped and fail-safe, but the `jagnelo` `provider-live-validation` environment cannot currently be inspected or populated from this session because the local `gh` credential is invalid for `jagnelo-symbiotech`; owner authentication is required before GitHub secret/variable presence can be verified
  - provider-specific incomplete contracts (FRED v1's published 120-requests/minute threshold is now paired with explicit fail-closed review controls for scope, conservative limit, and series terms; Nasdaq Trader polling; xStocks' unpublished public quota plus its official US-person/jurisdiction and redistribution restriction; Bybit xStocks endpoint/UID limits and provider-native header state; and FINRA OTC source terms/polling/redistribution approval) remain non-routable until reviewed; the separate FRED v2 2-requests/second rule is not applied to the v1 adapter; Tiingo/FMP bandwidth enforcement is implemented fail-closed and requires a complete operator-reviewed per-operation byte-bound map before promotion; Tiingo's provider-specific 500-unique-symbol monthly pool now has a durable per-window identity ledger and is no longer approximated as request count; Binance historical OHLCV now computes and reserves the conservative documented multi-page request weight before execution; FINRA synchronous short-interest, OTC Daily List, and the configurable OTC Security Master path now record the documented 1,200/minute/IP and 3 MB synchronous platform limits, with the credentialed datasets reserving the 3 MB maximum against the 10 GB monthly budget and settling to observed bytes; FINRA async signed-result accounting is implemented and fixture-tested, but remains default non-routable until a positive reviewed `FINRA_ASYNC_MAX_RESULT_BYTES` bound is supplied; the current FINRA API Terms of Service and dataset-specific redistribution/authorized-user boundary are recorded as a separate legal gate; every registered synchronous provider adapter now reports observed HTTP request counts, response bytes, and selected rate-limit headers into the runtime telemetry context; Finnhub's observed free key is recorded at 60/min plus the documented 30/sec hard cap, but its stock-candle endpoint is not entitled and remains excluded; Bybit's official 600/5-second/IP outer ceiling and rolling per-second endpoint/UID model are recorded, while the current public xStocks edge omitted the documented `X-Bapi-Limit*` headers during live validation, so no missing native state is guessed
  - complete production NMS/OTC reconciliation and the separately approved 30-day shadow run remain post-deployment gates; the NMS/SEC directory adapters and bounded keyless evidence are implemented, while FINRA OTC Daily List now supplies lifecycle deltas but OTC still needs an approved complete security-master source
progress:
  completed:
    - concrete opt-in adapters for Tiingo, Twelve Data, Finnhub, Marketstack, EODHD, FMP, Tradier, MarketData.app, Coinbase, and Kraken with fixture normalization
    - official Nasdaq Trader equity/ETF directory ingestion; undocumented Nasdaq quote-history routes removed
    - NMS pagination now requires explicit totals/cursor completion evidence; Nasdaq Financial Status is retained without treating deficient/bankrupt listings as delisted
    - exchange-qualified discovery rows cannot fall back to ticker-only cross-venue merges; failed/partial reconciliation runs report non-complete aggregate status
    - provider-specific quota contracts, multi-dimensional durable reservations, and fail-closed unknown-limit routing
    - OAuth-shaped FINRA client configuration and typed provider-not-configured/rate-limit failures
    - FINRA Query API dataset shape, OAuth expiry handling, documented synchronous quota contract, and credential-required entitlement
    - FINRA OTC Daily List lifecycle-event normalization with explicit delta-feed versus complete-universe boundary
    - fail-closed configurable FINRA OTC symbol-directory pagination adapter with the official synchronous request/payload contract; no default source URL or unreviewed terms are invented
    - worker-only universe lifecycle observations, repeated-missing deactivation, and exchange-aware core D1 coverage
    - paid-routing switches cannot bypass blank or unreviewed provider entitlements
    - admin status exposes exact missing quota-contract dimensions, entitlement review state, and weighted/credit operation-cost readiness
    - provider-policy status exposes required and missing environment-variable names without returning secret values
    - provider-policy status separately exposes required and missing routing-safety control variables for FINRA async bytes and Tiingo/FMP operation-byte maps
    - README provider-routing examples match the API-first Alpaca/EDGAR/OpenFIGI defaults and keep yfinance limited to disabled legacy/options compatibility
    - typed 429/418/quota capacity events persist reset headers and retry timestamps and are exposed through backend admin diagnostics
    - Binance exact request-weight accounting for single-symbol price (2) and exchange-info discovery (20); weighted local buckets charge the documented units and historical candles reserve their calculated multi-page weight before execution
    - Coinbase and Kraken crypto OHLC pagination now follows each provider's documented candle-page ceiling (300 and 720 respectively); explicit/latest/bulk paths reserve the calculated page count before execution instead of silently truncating history or charging one request
    - Twelve Data time-series history now follows the documented 5,000-point response ceiling using bounded start/end pages; explicit/latest/bulk paths reserve the conservative page count against its credit windows before execution
    - Marketstack EOD history now follows returned 100-row pagination metadata with progress guards; explicit/latest/bulk paths reserve a conservative calendar-day page count against the monthly request budget
    - Marketstack's conservative 100-request/month free-plan contract and IBKR's documented 10-request/second plus five-concurrent-history pacing contract
    - MarketData.app now uses the documented `/v1` API root, records its 100-credit 09:30 America/New_York reset window, and enforces the 50-request account-wide concurrency ceiling through a durable release-only in-flight quota dimension; adapter request-path/auth and concurrency regressions are covered
    - CIK evidence is issuer-only: profile ingestion links normalized SEC CIKs to `Issuer`, generic security domain-key selection excludes CIK, and new instrument-level CIK writes are rejected while legacy lookup remains compatible
    - deployed ARQ worker whole-universe refresh now enqueues durable bounded jobs and processes them every 15 minutes instead of making an unbounded provider-call loop inside the daily cron
    - final combined backend unit and PostgreSQL/Redis integration validation
    - savepoint-safe durable quota-window creation under concurrent workers, with full seeded Compose/browser validation
    - complete direct-transport instrumentation across registered synchronous adapters, including OpenFIGI's pooled HTTP client, so durable request logs do not undercount calls, response bytes, or provider headers
    - provider usage diagnostics expose the latest filtered provider-header snapshot and active durable quota-window reservations alongside request, unit, and byte aggregates
    - capacity-event persistence retains Binance weight and Bybit V5 limit/reset headers without retaining sensitive credentials
    - FINRA asynchronous Query API submit/status/presigned-result flow with OAuth on API legs and no Authorization header on the signed download leg
    - FINRA asynchronous presigned-result downloads now fail closed without a positive operator-supplied byte bound, validate declared and measured sizes, never forward OAuth credentials, and settle measured bytes against the durable monthly credential budget when a reviewed bound is configured
    - Twelve Data, Tradier, and Binance provider-native cumulative usage headers are retained and reconciled into their matching durable quota windows only when the observed values prove the reviewed contract limit; stale or mismatched observations cannot reduce local accounting
    - Tiingo's documented first-of-month Eastern bandwidth reset is represented explicitly and durable reservation windows honor the America/New_York calendar boundary; distinct provider symbols are now claimed once per durable monthly window, while reviewed per-operation byte reservations remain required for routing
    - FMP's operator-verified 512 MiB bandwidth pool is represented as a rolling 30-day constraint; FMP remains fail-closed until a reviewed per-operation byte reservation exists
    - Massive's documented `reference/ipos` and `marketstatus/upcoming` endpoints are normalized into the shared market-event surfaces with inclusive date bounds, IPO status filtering, holiday/early-close exchange metadata, verbatim payload provenance, and explicit cursor-page continuation; each one-request operation has its own quota cost and never triggers hidden pagination or compound reads
    - tokenized provider transport now converts network, HTTP, rate-limit, and invalid-JSON failures into redacted typed errors; only allow-listed rate headers are retained, `Retry-After` is parsed for retry metadata, and the finite Robinhood retry consumes the typed 429 path without leaking raw httpx failures
    - tokenized live validation now covers the xStocks and Robinhood corporate-action endpoints with positive transport observations; Robinhood event normalization filters malformed rows before optional symbol matching, with fixture coverage for bounded pagination/filter semantics
    - Tiingo/FMP byte-budget promotion is now explicit and provider-specific: complete positive operation-bound maps move the documented bandwidth pool into durable multidimensional reservations and measured-byte settlement; Tiingo's separate unique-symbol constraint is enforced through the durable identity ledger, and missing or partial maps remain non-routable without guessed ceilings
    - provider-defined and otherwise unspecified quota reset labels use conservative rolling windows rather than invented UTC epoch buckets; explicit calendar/session resets remain provider-specific
    - FINRA OTC Security Master now has the official synchronous 1,200-requests/minute/IP and 3 MB response contract; response-dependent cold refreshes require a positive reviewed operation-cost map, and independent source-terms, completeness, redistribution, and polling controls remain required before routing
    - FINRA OTC DAPI pagination now continues safely after provider payload-capped short pages, using authoritative record totals and progress checks instead of assuming every page reaches the requested row limit
    - Nasdaq's two-file directory refresh reserves two transport calls for both discovery and reconciliation operation families; FINRA OTC's response-dependent partition/page count requires reviewed operation costs and remains fail-closed by default rather than being charged as one request
    - transport telemetry retains FINRA pagination and payload-ceiling headers (`record-total`, effective limits, offsets, and response payload maximum) alongside existing provider-native usage headers
    - Robinhood tokenized-price reads use a finite provider-specific 429 retry budget with `Retry-After` support; repeated throttles remain observable and fail rather than entering a retry storm
    - Robinhood tokenized-price quota reservations now charge the asset lookup plus the bounded three-attempt quote retry worst case (four requests), rather than assuming every quote read is two calls
    - provider error messages are centrally redacted before durable request-log, health, capacity-event, or live-probe failure surfaces; configured secret values and credential-bearing URL/header values are covered by focused regression tests
    - Nasdaq Trader directory polling preserves provider `ETag`/`Last-Modified` validators and reuses parsed files on `304 Not Modified`; this reduces bandwidth without inferring an undocumented polling quota, so routing remains gated on the unknown contract
    - local and RPi Compose backend/worker environments now pass the FINRA async result bound and Tiingo/FMP operation-byte maps; research-runner receives none, with regression coverage for the trusted-process boundary
    - Bybit V5 response-limit headers are reconciled into durable usage totals only when they exactly match the reviewed coarse outer contract; endpoint/UID limits remain dynamic and fail-closed. Gate's official public TradFi stock contract is now recorded as 5 requests/second per IP endpoint family, with a conservative aggregate capability window and no invented response-header dependency
    - tokenized live probes require positive transport request/response-byte observations for each public tokenized adapter discovery and quote/order-book read, alongside normalized metadata/quote assertions
    - tokenized corporate-action refresh now persists xStocks history/upcoming and Robinhood combined action rows as provisional canonical MarketEvent records through the durable provider runtime; explicit asset-ID or unique-token-symbol linkage is used, unresolved rows remain visible, and adapters without an action endpoint are reported without invocation
    - tokenized corporate actions now have a dedicated `TOKENIZED_CORPORATE_ACTIONS` capability, provider protocol, registry resolver, PostgreSQL enum migration, default chain seed, and provider-availability representative probe; only xStocks and Robinhood advertise it, while catalogue/quote-only tokenized adapters remain unroutable for event reads
    - dedicated capability regression coverage passes 47/47 focused tests; Ruff, compilation, and diff checks pass; the authoritative Docker-backed combined backend gate passes 1855/1855 with 80.37% line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session `5ba942a1-788a-4f7b-812c-235563a717b0`, cleaned without host-wide pruning; the credentialed tokenized live suite passes 7/7
    - FRED HTTP 429/418 responses are preserved as typed capacity failures with provider headers and parsed Retry-After timestamps instead of being converted into empty-series/empty-price results; the v1 documented 120-requests/minute threshold is recorded while enforcement scope, adjustable limits, and terms remain unknown, and the separate v2 2-requests/second rule is not misapplied to v1
    - FRED v1's official errors documentation is now reflected as a 120-requests/minute quota dimension; explicit `FRED_REVIEWED_LIMIT_SCOPE`, `FRED_REVIEWED_REQUESTS_PER_MINUTE` (bounded to 1..120), and `FRED_SERIES_TERMS_REVIEWED` controls can replace those unknown dimensions only after operator review, while their defaults keep FRED excluded from routing
    - FRED reviewed-admission controls are wired through backend/RPi Compose, the manual GitHub live workflow, environment examples, provider diagnostics, and live preflight; defaults remain fail-closed and the complete control set is unit-tested as routable only under explicit review
    - FINRA OTC reviewed-admission controls are wired through backend/RPi Compose, the manual GitHub live workflow, environment examples, provider diagnostics, and live preflight; defaults remain fail-closed and response-dependent pagination is never charged through a generic one-request fallback
    - provider resolution and the provider-admin routing-eligibility diagnostic enforce every registered provider's non-secret routing-control set; missing operation costs, byte bounds, terms, polling, or other provider-specific admission controls cannot be bypassed by a configured source or credential
    - the manifest-driven live runner now takes an external, non-secret local key-use lock before invoking pytest; concurrent worktrees fail closed with exit code 3 instead of spending the same provider credentials concurrently, while cross-host/account usage remains explicitly outside the local lock's visibility
    - direct live probes now append measured per-provider operation/request/response-byte totals to an external JSONL usage ledger across sessions; the ledger is supplemental evidence for direct adapter consumption and never substitutes for provider-native or application-runtime quota accounting
    - backend provider-usage diagnostics optionally read that redacted external ledger and expose separate live-test totals/status per provider; the file is deployment-mounted/configured explicitly, never merged into runtime reservations, and never required for routing
    - provider-usage summary regression proves cross-session live-ledger totals remain separate from runtime request counts and durable quota-window availability; focused usage/router/live-ledger tests pass 13/13 and the authoritative Docker-backed gate passes 1860/1860 with 80.38% line coverage
    - the manual GitHub live workflow writes the redacted live ledger to runner temp storage and uploads it for 90-day operator reconciliation under an always-run artifact step; provider secret-wiring tests enforce the contract and routine CI remains secret-free
    - scripts/merge-provider-live-usage.py provides lock-protected, allow-listed, deduplicating reconciliation of downloaded local/GitHub receipts without changing routing or runtime reservations; focused receipt/secret-wiring tests pass 12/12 and the authoritative gate passes 1862/1862
    - external live-ledger parsing and receipt merging accept only non-negative integers or digit strings; booleans, fractions, negatives, and other coercible usage values are rejected; focused strict-parser tests pass 7/7 and the authoritative gate passes 1862/1862 with 80.38% line coverage
    - Alpaca, FRED, and SEC EDGAR transport failures now raise redacted typed ProviderResponseError values instead of becoming empty/synthetic results; focused provider tests pass 106/106 and the authoritative gate passes 1865/1865 with 80.39% line coverage
    - Alpaca, FRED, and SEC EDGAR malformed or structurally invalid JSON payloads now raise typed ProviderResponseError values; focused provider tests pass 109/109 and the authoritative gate passes 1868/1868 with 80.35% line coverage
    - Coinbase, Kraken, and OpenFIGI network, malformed-JSON, and invalid-shape failures now raise typed ProviderResponseError values; focused crypto/OpenFIGI/provider-transport tests pass 116/116 and the authoritative gate passes 1872/1872 with 80.35% line coverage
    - Alpha Vantage and CoinGecko network and malformed-JSON failures now raise typed ProviderResponseError values through shared helpers; focused provider/transport tests pass 115/115 and the authoritative gate passes 1876/1876 with 80.36% line coverage
    - Binance, Nasdaq Trader, FINRA Query API, and the FINRA OTC directory now raise redacted typed ProviderResponseError values for transport, malformed JSON, invalid shapes, and incomplete directory payloads; Binance rejects malformed candle rows instead of silently dropping them, and Nasdaq accepts official ACT Symbol plus approved Symbol mirror headers with identity validation. Focused provider/directory/instrumentation tests pass 138/138 and the corrected authoritative gate passes 1885/1885 with 80.37% line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session 43c93bbd-d914-4259-ad39-21197ad67ed7, cleaned without host-wide pruning
    - FINRA bounded asynchronous presigned-result downloads now raise redacted typed ProviderResponseError values for stream setup/iteration transport failures while retaining positive byte-bound and credential-free signed-download enforcement; focused FINRA tests pass 9/9 and the authoritative gate passes 1886/1886 with 80.36% line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session 0f4c9b7f-f9a2-43e6-a9a6-9a1cfa2fb7bd, cleaned without host-wide pruning
    - FRED missing credentials raise the typed provider-not-configured failure instead of returning an empty series/price, preserving honest runtime fallback semantics
    - Alpaca missing API/secret credentials now raise the typed provider-not-configured failure across history, latest-price, event, and discovery operations instead of masquerading as empty data; unsupported symbols/timeframes retain their legitimate empty-result behavior
    - Alpaca and SEC EDGAR now propagate upstream HTTP status failures instead of converting rejected requests into empty/synthetic data; the centralized runtime can therefore record typed capacity events and reset metadata consistently
    - SEC EDGAR now rejects a blank deployment User-Agent with ProviderNotConfiguredError; every local, CI, and deployed environment must supply its own descriptive non-secret contact value
    - optional REST adapters now reject explicit HTTP-success error envelopes as typed provider-response failures, and classify rate/quota wording as typed capacity failures instead of returning false empty data
    - manifest-driven live probes require positive transport observations for every available non-tokenized adapter read, with SEC/Nasdaq directory pagination explicitly cache-aware
    - the HTTP-success provider error-envelope guard is covered by the full Docker-backed gate (`1779/1779`, `80.14%`), preserving typed failures instead of false empty observations
    - tokenized public adapters now reject Kraken non-empty error lists and Bybit nonzero retCode HTTP-success envelopes as typed failures, preserving registered provider identity and preventing synthetic token records
    - provider resolution now applies an explicit provider/instrument-class allow-list before invocation, so equity/ETF requests cannot fall through to crypto-only OHLCV adapters; unknown provider mappings fail closed and the regression is covered by unit and PostgreSQL-backed OHLCV tests
    - tokenized-security instruments created by `upsert_tokenized_asset` are explicitly admitted to the five tokenized providers while remaining excluded from ordinary equity routing; the Gate public TradFi stock contract is now recorded as the documented 5-qps/IP static contract with a conservative aggregate capability window
    - persisted tokenized products now have an opt-in bounded quote-refresh path; quote requests route through the durable provider runtime using provider asset IDs as usage identities, retain separate token-instrument price snapshots, remain disabled by default in the worker schedule, and reserve two upstream requests for adapters whose quote operation resolves metadata before reading the quote/order book
    - CoinGecko metadata resolution now uses the ranked provider search endpoint instead of arbitrary `/coins/list` ordering, preventing ambiguous tickers such as BTC from resolving to an unrelated token; the compound search-plus-profile operation reserves and live-verifies two upstream requests
    - Alpaca paginated OHLCV history now estimates and reserves every conservative 1,000-bar page for explicit and latest-range fetches, including bulk backfill, instead of charging one request for an unbounded `next_page_token` loop
    - FINRA authenticated short-interest and Daily List calls now reserve the cold OAuth token request plus the dataset POST (two requests), while warm token-cache calls remain safely over-reserved
    - SEC EDGAR profile and filing-event operations now reserve the cold ticker-directory lookup plus submissions request (two requests), with live transport evidence for the compound metadata path
    - the complete Docker-backed gate after class-aware routing passes (`1785/1785`, `80.11%`), including the two previously failing AAPL OHLCV integration cases
    - Tradier's documented nested `history.day|week|month` and `securities.security` JSON wrappers, including singleton-object forms, are normalized by the concrete adapter and covered by provider fixtures
    - EODHD's documented EOD `period=d|w|m` contract is exposed as daily, weekly, and monthly canonical timeframes, with provider-path fixture assertions for the weekly and monthly requests
    - Twelve Data's documented timezone semantics are honored: intraday requests explicitly request UTC, while daily/weekly/monthly rows are interpreted in the returned exchange timezone before canonical UTC storage; fixture and bounded live coverage exercise both paths
    - Finnhub's documented forward earnings calendar is normalized as a bounded `market_events` surface, with date-range filtering, fixture coverage, and a configured live profile-plus-history-plus-calendar probe
    - Alpha Vantage's documented IPO calendar is represented as provisional bounded `market_events`; capability discovery and date filtering are fixture-covered, and the dedicated live case now validates the provider's CSV transport/schema even when the current window has no published rows
    - Alpha Vantage's reviewed usage profile explicitly prices every current single-query operation (search, daily/latest history, current price, listing discovery, and IPO events) at one request; adapter changes that add pagination or compound lookups must update this map before routing
    - single-request operation profiles are now explicit for Alpaca, Massive, FRED, OpenFIGI, Coinbase, Kraken, Marketstack, Finnhub, FMP, Tiingo, and Tradier; range/pagination-dependent history remains caller-estimated or byte-bound gated rather than inheriting a generic one-request charge
    - Marketstack ticker discovery no longer silently defaults to XNYS; it requires an explicit operator-selected MIC/exchange setting and remains supplementary to complete NMS/OTC reconciliation
    - the live runner and GitHub workflow now surface/pass Marketstack's non-secret venue scope separately from its API key, so a history probe cannot hide a discovery admission gap
    - Optional-provider HTTP status/request failures are converted to redacted typed errors; 429/418 responses retain provider reset headers, `api_token` URLs are covered by the credential scrubber, and EODHD/FMP metadata live outcomes are entitlement-specific rather than false empty successes
    - EODHD Fundamentals now uses the provider's documented `/api/v1.1` endpoint rather than the EOD `/api` root; the path is fixture-covered and the supplied free key's 403 remains an explicit entitlement result
    - Bybit xStocks live validation confirms the public instrument and ticker reads and records the observed absence of the documented endpoint/UID response headers; telemetry remains allow-listed and routing stays fail-closed rather than inventing endpoint limits
    - the manifest live-runner safety preflight now reports xStocks' official US-person/jurisdiction/redistribution restriction alongside its unpublished public quota, so a successful public probe cannot be mistaken for US routing eligibility
    - provider-admin quota edits now reject non-positive numeric limits and require an explicit complete replacement quota contract plus provenance; numeric policy changes cannot silently diverge from the documented provider contract
    - quota-contract governance now keeps incomplete operator replacements explicitly unverified; admin diagnostics cannot stamp unknown/untracked provider constraints as reviewed, while complete contracts retain their verification timestamp and fail-closed routing behavior
    - existing provider policies are also repaired during runtime seeding: stale quota_verified_at values are cleared whenever the current contract contains unknown or untracked dimensions, with regression coverage for FRED and Tiingo/FMP bandwidth gates
    - complete quota-contract replacements now require policy-level quota_scope and quota_source in addition to per-dimension fields; missing scope/provenance cannot receive a verification timestamp
    - runtime admission now applies the same policy-level scope/source requirement as the admin PATCH path; migrated or externally edited rows with missing provenance are explicitly non-routable, while unknown/untracked constraints remain auditable but unverified
    - operator-cleared quota_scope/quota_source values are not silently reseeded on later diagnostics reads; provenance edits clear quota_verified_at immediately and require a fresh complete replacement contract
    - operator-cleared quota_contract values are likewise preserved across diagnostics and seeding; an explicit NULL contract remains a routing quarantine until a complete, provenance-backed replacement is supplied
    - optional providers' inherited latest-price paths now have explicit `get_current_price` operation costs; Tiingo/FMP byte-bound controls include that bounded latest-history request, and Marketstack no longer relies on an implicit one-request profile
    - deep-history `bulk_fetch:*` calls now have explicit operation costs for EODHD, MarketData.app, Tiingo, and FMP; Tiingo/FMP reviewed byte-bound maps must include `bulk_fetch` before those workers can route, so the history worker cannot silently undercharge or bypass bandwidth admission
    - provider quota admission now fails closed for every unknown operation, including simple request-count contracts; Alpha Vantage, FRED, Finnhub, and Tradier deep-history calls plus the FRED risk-free-rate path have explicit operation costs, while test fixtures must declare their reviewed operation map
    - the authoritative Docker-backed gate after strict operation-cost admission passes `1835/1835` with `80.33%` line coverage and 89 warnings; isolated PostgreSQL/Redis testcontainer session `bb89a147-2fde-41cf-8141-c7edce0ca476` was cleaned without host-wide pruning
    - operation-cost admission rejects malformed, non-positive, boolean, and non-finite reviewed values and caller overrides instead of coercing them to one unit; the final authoritative gate remains green at `1835/1835`
    - the execution path now passes caller overrides through the same strict operation-cost validator and no longer applies a post-admission one-unit fallback; missing or invalid costs raise `ProviderQuotaUnknownError` before reservation
    - the post-execution strict-cost gate passes `1835/1835` with `80.34%` line coverage and 89 warnings; isolated PostgreSQL/Redis testcontainer session `70860336-c753-4b11-bdbf-72e643772e40` was cleaned without host-wide pruning
    - required non-request dimension bounds are now validated as positive integral reviewed values during both admission and reservation; malformed, fractional, or non-positive byte/credit/record bounds cannot pass admission or be coerced during settlement
    - the strict dimension-bound regression gate passes `98/98` focused quota/runtime/registry/support tests and the authoritative Docker-backed gate passes `1835/1835` with `80.33%` line coverage and 89 warnings; isolated PostgreSQL/Redis testcontainer session `afffe209-75ec-4338-b6df-92529fddc5f0` was cleaned without host-wide pruning
    - Tiingo/FMP routing-control diagnostics now consume the same complete six-operation byte-bound set used by runtime promotion (`fetch_ohlcv`, `fetch_latest_ohlcv`, `get_current_price`, `bulk_fetch`, and the provider-specific discovery/profile operations); partial maps remain visibly non-routable instead of appearing complete
    - the aligned byte-bound diagnostics gate passes `98/98` focused quota/runtime/registry/support tests and the authoritative Docker-backed gate passes `1835/1835` with `80.33%` line coverage and 89 warnings; isolated PostgreSQL/Redis testcontainer session `fed69565-180c-4121-b1da-083621617313` was cleaned without host-wide pruning
    - byte-bound controls now reject JSON booleans and other non-integral values consistently in settings promotion, provider-admin diagnostics, and live routing preflight; the focused regression suite passes `105/105`, and the authoritative Docker-backed gate passes `1835/1835` with `80.33%` line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session `e88c0ea3-1c1e-4746-ad8b-38658f706f4f`, cleaned without host-wide pruning
    - FINRA async result bounds, FINRA OTC polling intervals, and FRED reviewed request limits now use the same strict positive-integer validation as operation and byte costs; booleans and non-integral values cannot promote provider profiles or routing, the focused registry/quota/secret suite passes `79/79`, and the authoritative Docker-backed gate passes `1836/1836` with `80.34%` line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session `8d2f1c1b-b568-49d3-9c42-6906d00ba07e`, cleaned without host-wide pruning
    - reviewed provider terms flags now require actual boolean `true` values in both quota seeding and routing diagnostics; truthy strings cannot satisfy FINRA OTC or FRED review gates, the focused registry/quota/secret suite passes `80/80`, and the authoritative Docker-backed gate passes `1837/1837` with `80.34%` line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session `12ddd0df-d4ab-4ec5-bc05-59f85bd92c0c`, cleaned without host-wide pruning
    - FMP now exposes the stable `earnings-calendar` endpoint through the `market_events` capability; fixture coverage preserves provider EPS/revenue fields and inclusive date bounds, the FMP operation-cost and byte-bound contracts include `fetch_market_events`, the configured live profile/history/calendar probe passed with 3 upstream requests and 9,444 response bytes, and the authoritative Docker-backed gate passed `1839/1839` with 80.35% line coverage and 89 warnings using isolated PostgreSQL/Redis testcontainer session `c2dc85cf-f806-4840-89a7-bbded5c76194`, cleaned without host-wide pruning
  total: 15
  current_phase: credentialed_live_validation
  current_blocker: the latest complete live matrix at 2026-09-10T04:57:59Z passes 31/34 in 38.13 seconds with temporary non-secret EDGAR_USER_AGENT and MARKETSTACK_DISCOVERY_EXCHANGE=XNAS overrides; all seven tokenized public probes pass, including both corporate-action reads, while the three exact credential domains (ALPACA_API_KEY/ALPACA_SECRET_KEY, TRADIER_API_KEY, MARKETDATA_APP_API_KEY) remain absent. The GitHub `jagnelo` live environment is not externally verifiable until owner authentication is restored; Alpha Vantage's live IPO-calendar case currently proves only a valid header-only empty response (no positive event row), while FRED's three explicit review controls (scope, conservative limit, and series terms), FINRA OTC's positive operation-cost map plus terms/completeness/redistribution/poll controls, Nasdaq polling allowance, xStocks public quota and US-person/jurisdiction/redistribution eligibility, Bybit xStocks endpoint/UID limits, complete reviewed Tiingo/FMP byte maps including `bulk_fetch`, complete NMS/OTC reconciliation, and shadow activation remain post-deployment or operator-review gates; each deployment must also provide its own non-secret SEC User-Agent
  next_action: restore owner authentication for the `jagnelo` GitHub account, verify or populate the environment's provider secrets and safety variables, obtain the three missing credential domains, configure a deployment-owned SEC User-Agent, run the complete manifest live matrix, then reassess readiness; do not activate routing or deploy before that evidence, and do not promote Tiingo/FMP until complete reviewed byte-bound maps are supplied
